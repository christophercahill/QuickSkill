<br><br>

<div class="container">

    <ol class="breadcrumb">
     
    </ol>

    <div class="row">

      <!-- Article main content -->
      <article class="col-md-12 maincontent">
        <header class="page-header">
<div class="title">
         <h1 class="page-title text-center"><strong>All Posts</strong></h1>
         <h2 class="intro-text">Search All Posts
                </h2>
                <hr>
                <%= form_tag(posts_search_path, :method => "get", id: "search-form") do %>
                <%= text_field_tag :search, params[:search] %>
                <%= submit_tag "Search", :name => nil %>
                <% end %> </h2>
                <hr>
                </div>
          <% @posts.each do |post| %>
                    <% user_id = post.user_id %>
                    <% @user = User.find(user_id) %>
                    <%  @profile = @user.profile %>
        
        </header>
<h2><%= link_to post.title, post_path(post), class: 'link-to-text-color' %></h2>
        <h3><%= @profile.name%></h3>
        <p><%= post.description %></p>
        <hr>
        <%end%>


      </article>
<!-- /.container -



<div class="container">

    <div class="row">
        <div class="box">
            <div class="column-lg-12 text-center">
                <hr>
                <h2 class="intro-text text-center">All
                    <strong>posts</strong>
                </h2>
                <hr>
                <h2 class="intro-text text-center">Search All Posts
                </h2>
                <%= form_tag(posts_search_path, :method => "get", id: "search-form") do %>
                <%= text_field_tag :search, params[:search] %>
                <%= submit_tag "Search", :name => nil %>
                <% end %> </h2>
                <hr>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="box">
                <div class="col-lg-12 text-center">
                	<% @posts.each do |post| %>
                    <% user_id = post.user_id %>
                    <% @user = User.find(user_id) %>
                    <%  @profile = @user.profile %>
                    <h2><%= link_to post.title, post_path(post), class: 'link-to-text-color' %>
                    </h2>
                    <div class="name"><h3><%= @profile.name%></h3></div>
                    <p><%= post.description %></p>
                    
                    <hr>
                    <% end %>
                </div>
            </div>
        </div>

</div>
    <!-- /.container -->










