<p>
  <strong>Review:</strong>
  <%= review.body %>
</p>

<p>
<% if current_user.id == review.user_id %>
<%= link_to 'Edit', edit_profile_review_path(review.profile, review) %>
  <%= link_to 'Delete', [review.profile, review],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
<% end %>

</p>